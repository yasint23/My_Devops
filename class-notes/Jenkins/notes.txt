* Terminalden jenkins server icerisine girmek/baglanmak icin sum komutlari girilir;

sudo usermod -s /bin/bash jenkins
sudo su - jenkins

* jenkinsIP:8080/env-vars.html/ browserda bu adresten jenkins in env variableri gorebiliriz